"""Factory root test package.

These tests cover the portal runtime (scripts/) rather than any
generated project under projects/. pytest.ini excludes projects/ from
discovery so this suite only exercises the factory itself.
"""
