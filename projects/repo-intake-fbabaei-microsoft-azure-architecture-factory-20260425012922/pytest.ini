[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Don't pick up generated-project tests under projects/ when running
# the factory test suite.
norecursedirs = projects .venv .git outputs logs .squad factory-templates
addopts = -q --tb=short
