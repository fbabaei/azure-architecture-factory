apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: aks-micro-demo
  labels:
    app: order-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: REPLACE_WITH_ACR/order-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: aks-micro-config
          env:
            - name: AKS_DEMO_SERVICE_NAME
              value: order-service
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: aks-micro-demo
spec:
  selector:
    app: order-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
