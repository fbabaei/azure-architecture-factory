# Source Layout

The runnable application code for this sample currently lives under `web/`.

This `src/` folder exists so the sample aligns with the repository's standard lifecycle structure and can be expanded with service-oriented modules over time.
