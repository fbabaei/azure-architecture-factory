# Portal runtime dependencies (optional — portal is stdlib-only without these)
#
# When installed, these enable Azure Monitor / Application Insights telemetry.
# The portal auto-detects availability and falls back to no-op tracing
# when this set is not installed OR when APPLICATIONINSIGHTS_CONNECTION_STRING
# is not set.
#
# To install:
#   pip install -r scripts/requirements.portal.txt
#
# To build a lean image without telemetry, omit this from Dockerfile.portal.

azure-monitor-opentelemetry>=1.6.0,<2.0.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0

# Azure Identity — enables Entra ID auth for Azure OpenAI when
# disableLocalAuth=true (no API key). Falls back gracefully when missing.
azure-identity>=1.19.0,<2.0.0

# Azure Identity — enables Entra ID auth for Azure OpenAI when
# disableLocalAuth=true (no API key). Falls back gracefully when missing.
azure-identity>=1.19.0,<2.0.0
